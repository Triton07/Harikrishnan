Write a C program to print reverse of an array:

#include <stdio.h>    
int main()  
{  
    int a[100], i,n;
	scanf("%d",&n);  
    printf("Enter n integer numbers\n");  
    for(i = 0; i < n; i++)  
        scanf("%d", &a[i]);  
     printf("Array elements are:\n");  
    for(i = n; i >= 0; i--)  
        printf("%d\n",a[i]);  
  
    return 0;  
}     
