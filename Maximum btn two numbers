Write a c program to find the maximum btn two numbers:

#include<stdio.h>
int main()
{
	int a,b,*p,*q;
	printf("Enter the value of A = ");
	scanf("%d",&a);
	printf("Enter the value of B = ");
	scanf("%d",&b);
	p=&a;
	q=&b;
	if(*p>*q)
	{
		printf("%d is the maximum number",*p);
	}
	else if(*p==*q)
	{
		printf("The given numbers are equal");
	}
	else 
	{
		printf("%d is the maximum number",*q);
	}
	return 0;
}
Test Cases :

Case 1:
Enter the value of A = 0
Enter the value of B = 4
4 is the maximum number

Case 2:
Enter the value of A = 5
Enter the value of B = 0
5 is the maximum number

Case 3:
Enter the value of A = -3
Enter the value of B = 3
3 is the maximum number

Case 4:
Enter the value of A = 0
Enter the value of B = 0
The given numbers are equal

Case 5:
Enter the value of A = 123
Enter the value of B = 123
The given numbers are equal
